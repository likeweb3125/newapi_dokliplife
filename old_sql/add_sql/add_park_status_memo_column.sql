-- =============================================
-- parkStatus 테이블 컬럼 추가 SQL
-- =============================================

-- memo 컬럼 추가
-- 차량번호나 차종 등 주차 관련 메모를 저장하는 컬럼
-- 주의: 이미 컬럼이 존재하는 경우 오류가 발생할 수 있습니다.
ALTER TABLE `parkStatus` 
ADD COLUMN `memo` VARCHAR(500) NULL COMMENT '메모 (차량번호, 차종 등)' 
AFTER `useEndDate`;

-- =============================================
-- 참고사항
-- =============================================

-- memo: 주차 상태에 대한 추가 정보를 저장합니다.
--   - 차량번호: 예) "12가3456"
--   - 차종: 예) "소나타", "아반떼" 등
--   - 기타 주차 관련 메모 사항
